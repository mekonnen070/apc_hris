// Categorizes different types of documents.
enum DocumentType {
  cv,
  contract,
  id, // General ID
  passport,
  drivingLicense,
  academicCertificate,
  trainingCertificate,
  paySlip,
  taxDocument,
  medicalReport,
  other,
}